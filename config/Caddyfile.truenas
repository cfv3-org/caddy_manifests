{
    email admin@cfv3.org
    debug
    auto_https off
}

*.cfv3.org {
    header Content-Type application/json
    respond `{"error":"service not registered","host":"{http.request.host}","label":"{labels.0}"}` 200

#    reverse_proxy {labels.0}:80
#
#    handle_errors {
#        respond "Service not available" 503
#    }
}

#{
#    header Content-Type application/json
#    respond `{"error":"service not registered","host":"{http.request.host}"}` 200
#}
